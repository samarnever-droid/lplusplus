import sys
# THE ONLY FILE DIFFERENT BETWEEN ORIGIN/MASTER AND MY BRANCH IS src/pm.rs!
# Wait, I just compiled it with `cargo build --release`. Let's test the linker on `test_link.obj`.
# First generate `test_link.obj`.

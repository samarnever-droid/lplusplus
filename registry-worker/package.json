{
  "name": "lplusplus-registry-worker",
  "version": "1.0.0",
  "private": true,
  "description": "L++ Package Registry API — Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}

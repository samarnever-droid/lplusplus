# King 20 Stable v1

This is a **frozen historical baseline**. Its 20 workload IDs, sources, expected
stdout, and interpretation must never change. If L++ needs a new stable standard,
create `stable/v2/` rather than editing this directory.

Run:

```sh
python3 benchmarks/king20/run.py --suite stable
```

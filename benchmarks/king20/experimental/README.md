# King 20 Experimental

This suite is the active proving ground for new ownership semantics, regressions,
backend improvements, and future optimizations. It may change as the language
expands. A stable suite is cut from a verified experimental snapshot.

Run:

```sh
python3 benchmarks/king20/run.py --suite experimental
```

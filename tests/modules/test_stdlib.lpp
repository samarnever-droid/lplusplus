from stdlib.math import abs, min, max, pow, fib, factorial, is_even

def main():
    print(abs(42))
    print(min(10, 20))
    print(max(10, 20))
    print(pow(2, 10))
    print(fib(10))
    print(factorial(6))
    
    if is_even(42):
        print(1)
    else:
        print(0)

# ARC destructor regression: Parent owns a Child field.
# Releasing Parent must recursively release Child through generated destructors.
# Expected output:
# 1

struct Child:
    value: Int

struct Parent:
    child: Child

def main():
    child := Child()
    parent := Parent()
    parent.child = child
    print(1)

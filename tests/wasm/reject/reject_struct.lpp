# reject_struct.lpp — custom structs are not yet supported on wasm32;
# the compiler must fail with a clear "WebAssembly" diagnostic.

struct Point:
    x: Int
    y: Int

def main():
    p := Point(1, 2)
    print(p.x + p.y)

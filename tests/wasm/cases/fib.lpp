# fib.lpp — recursion (same program as the native AOT parity case).

def fib(n: Int) -> Int:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

def main():
    result := fib(10)
    print(result)
    print(fib(15))

# branches.lpp — if/else and comparison operators (same program as the
# native AOT parity case).

def classify(n: Int) -> Int:
    if n > 0:
        return 1
    return 0

def main():
    print(classify(42))
    print(classify(0))
    print(classify(100))

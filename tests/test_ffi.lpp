# FFI — calling C functions directly

extern "C":
    def abs(x: Int) -> Int
    def _getpid() -> Int

def main():
    # Call C stdlib abs()
    n := abs(-42)
    print(n)

    # Call _getpid()
    pid := _getpid()
    print(pid)

    print("ffi works!")

# Test stateful closures with mutable captures
def main():
    mut count := 0
    counter := fn() -> Int:
        count = count + 1
        return count

    print(counter())
    print(counter())
    print(counter())
